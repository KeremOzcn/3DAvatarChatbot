type Props = {
  setAssistantText: (text: string) => void;
};

export const Moshi = ({ setAssistantText }: Props) => {
  // Moshi is a placeholder component for future voice chat integration
  return null;
};
